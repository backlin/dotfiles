# https://apple.stackexchange.com/a/250572
ssh-add --apple-load-keychain -q

# Homebrew paths (Apple Silicon and Intel)
export PATH="/opt/homebrew/bin:$PATH"
export PATH="/usr/local/bin:$PATH"
export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"
export PATH="$HOME/nofuss/git/frontmatter:$PATH"

# Databricks Spark needs this
export JAVA17=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home/bin/java

# TeX Live (MacTeX distribution)
export PATH="/Library/TeX/texbin:$PATH"

# macOS-specific applications
export PATH="$HOME/google-cloud-sdk/bin:$PATH"
export PATH="/Applications/GoLand.app/Contents/MacOS:$PATH"

# Enable shell completions
autoload -U compinit; compinit
autoload -U +X bashcompinit && bashcompinit

# Azure CLI completion (if installed via Homebrew)
if [[ -f $(brew --prefix)/etc/bash_completion.d/az ]]; then
  source $(brew --prefix)/etc/bash_completion.d/az
fi

#---[ Aliases ]----------------------------------------------------------------

alias y=" pbcopy "
alias p=" pbpaste "
